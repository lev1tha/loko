# Production build (Cloudflare Pages). The frontend calls the backend API
# directly at its public domain — no Vite dev proxy in production.
# Set this to your backend domain exposed via Cloudflare Tunnel.
VITE_API_BASE_URL=https://api.loko.kg/api
